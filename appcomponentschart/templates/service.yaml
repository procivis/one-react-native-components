apiVersion: v1
kind: Service
metadata:
  name: {{ include "appcomponentschart.fullname" . }}
  labels:
    {{- include "appcomponentschart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "appcomponentschart.selectorLabels" . | nindent 4 }}
