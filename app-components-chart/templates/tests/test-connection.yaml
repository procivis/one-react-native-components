apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-components-chart.fullname" . }}-test-connection"
  labels:
    {{- include "app-components-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-components-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
