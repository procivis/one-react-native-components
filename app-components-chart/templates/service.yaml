apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-components-chart.fullname" . }}
  labels:
    {{- include "app-components-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-components-chart.selectorLabels" . | nindent 4 }}
